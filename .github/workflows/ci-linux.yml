name: CI - Linux

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

# Solo 1 workflow por PR (cancela los anteriores)
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ════════════════════════════════════════════════════════════════════
  # STATIC ANALYSIS - Fast feedback
  # ════════════════════════════════════════════════════════════════════
  static-analysis:
    name: Code Style & Linting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format clang-tidy

      - name: Check code formatting (clang-format)
        run: |
          echo "Checking code format..."
          find src include tests -name "*.cpp" -o -name "*.hpp" -o -name "*.h" | while read file; do
            if ! clang-format --Werror --dry-run "$file" > /dev/null 2>&1; then
              echo "❌ File needs formatting: $file"
              exit 1
            fi
          done
          echo "✅ All files are properly formatted"

      - name: Static analysis (clang-tidy)
        run: |
          echo "Running clang-tidy..."
          find src -name "*.cpp" -o -name "*.h" | head -5 | while read file; do
            if [ -f "$file" ]; then
              clang-tidy "$file" -- -I/workspace -std=c++20 || true
            fi
          done
          echo "✅ Static analysis complete"

      - name: Markdown analysis (markdownlint)
        run: |
          sudo npm install -g markdownlint-cli
          echo "Running markdownlint..."
          markdownlint '**/*.md' --ignore 'build/**' --ignore 'build-tsan/**' --ignore 'build-debug/**' --ignore 'build-coverage/**' || true
          echo "✅ Markdown analysis complete"

      - name: Shell scripts analysis (shellcheck)
        run: |
          sudo apt-get install -y shellcheck
          echo "Running shellcheck on all scripts..."
          find scripts -name "*.sh" -print0 | xargs -0 shellcheck
          echo "✅ Shell scripts are clean"

  # ════════════════════════════════════════════════════════════════════
  # CODE COVERAGE - Generates gcovr reports
  # ════════════════════════════════════════════════════════════════════
  code-coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    needs: static-analysis
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup build environment
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential cmake ninja-build \
            python3 python3-pip git curl \
            libcurl4-openssl-dev libssl-dev \
            libavcodec-dev libavformat-dev libavutil-dev \
            libswscale-dev libswresample-dev \
            libsdl2-dev libgl1-mesa-dev \
            gcovr lcov
          
          pip3 install --no-cache-dir conan
          conan profile detect --force
          echo "CC=gcc" >> $GITHUB_ENV
          echo "CXX=g++" >> $GITHUB_ENV
      
      - name: Conan install
        run: |
          cd $GITHUB_WORKSPACE
          conan install . --build=missing -s build_type=Debug
      
      - name: CMake configure (Coverage)
        run: |
          cd $GITHUB_WORKSPACE
          cmake -B build-coverage -DCMAKE_TOOLCHAIN_FILE=build/Debug/generators/conan_toolchain.cmake \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_CXX_FLAGS="--coverage -g -O0" \
            -DCMAKE_EXE_LINKER_FLAGS="--coverage" \
            -DCMAKE_SHARED_LINKER_FLAGS="--coverage"
      
      - name: Build
        run: |
          cd $GITHUB_WORKSPACE
          cmake --build build-coverage --parallel $(nproc)
      
      - name: Run tests
        run: |
          cd $GITHUB_WORKSPACE/build-coverage
          ctest --verbose --output-on-failure
      
      - name: Generate coverage report
        run: |
          cd $GITHUB_WORKSPACE
          mkdir -p build-coverage/reports
          gcovr -r . \
            -e "build-coverage/.*" \
            -e "build/.*" \
            -e "tests/.*" \
            -e "test/.*" \
            --gcov-ignore-parse-errors=negative_hits.warn \
            --html-details -o build-coverage/reports/coverage.html \
            --print-summary
            
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: build-coverage/reports/

  # ════════════════════════════════════════════════════════════════════
  # BUILD & TEST - Core checks
  # ════════════════════════════════════════════════════════════════════
  build-test:
    name: Build & Test (Debug)
    runs-on: ubuntu-latest
    needs: static-analysis
    
    strategy:
      fail-fast: true
      matrix:
        compiler: [ gcc, clang ]
    
    steps:
      - uses: actions/checkout@v7
      
      - name: Cache Conan packages
        uses: actions/cache@v6
        with:
          path: ~/.conan2
          key: conan-${{ runner.os }}-${{ matrix.compiler }}-${{ hashFiles('conanfile.txt') }}
          restore-keys: |
            conan-${{ runner.os }}-${{ matrix.compiler }}-
      
      - name: Select compiler
        run: |
          if [ "${{ matrix.compiler }}" = "clang" ]; then
            sudo apt-get install -y clang
            echo "CC=clang" >> $GITHUB_ENV
            echo "CXX=clang++" >> $GITHUB_ENV
          else
            echo "CC=gcc" >> $GITHUB_ENV
            echo "CXX=g++" >> $GITHUB_ENV
          fi
          
      - name: Setup build environment
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential cmake ninja-build \
            python3 python3-pip git curl \
            libcurl4-openssl-dev libssl-dev \
            libavcodec-dev libavformat-dev libavutil-dev \
            libswscale-dev libswresample-dev \
            libsdl2-dev libgl1-mesa-dev
          
          pip3 install --no-cache-dir conan
          conan profile detect --force
      
      - name: Conan install
        run: |
          cd $GITHUB_WORKSPACE
          conan install . --build=missing -s build_type=Debug
      
      - name: CMake configure (Debug with sanitizers)
        run: |
          cd $GITHUB_WORKSPACE
          cmake -B build-debug -DCMAKE_TOOLCHAIN_FILE=build/Debug/generators/conan_toolchain.cmake \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_CXX_COMPILER=${{ env.CXX }} \
            -DCMAKE_CXX_FLAGS="-fsanitize=address,undefined -fno-omit-frame-pointer -g"
      
      - name: Build
        run: |
          cd $GITHUB_WORKSPACE
          cmake --build build-debug --parallel $(nproc)
      
      - name: Run tests
        run: |
          cd $GITHUB_WORKSPACE/build-debug
          ctest --verbose --output-on-failure
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.compiler }}
          path: build-debug/

  # ════════════════════════════════════════════════════════════════════
  # THREAD SAFETY - TSan check
  # ════════════════════════════════════════════════════════════════════
  thread-safety:
    name: Thread Safety (TSan)
    runs-on: ubuntu-latest
    needs: static-analysis
    
    steps:
      - uses: actions/checkout@v7
      
      - name: Setup build environment
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential cmake ninja-build \
            python3 python3-pip git curl \
            libcurl4-openssl-dev libssl-dev \
            libavcodec-dev libavformat-dev libavutil-dev \
            libswscale-dev libswresample-dev \
            libsdl2-dev libgl1-mesa-dev
          
          pip3 install --no-cache-dir conan
          conan profile detect --force
      
      - name: Conan install
        run: |
          cd $GITHUB_WORKSPACE
          conan install . --build=missing -s build_type=Debug
      
      - name: CMake configure (TSan)
        run: |
          cd $GITHUB_WORKSPACE
          cmake -B build-tsan -DCMAKE_TOOLCHAIN_FILE=build/Debug/generators/conan_toolchain.cmake \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_CXX_FLAGS="-fsanitize=thread -fPIC -g"
      
      - name: Build with TSan
        run: |
          cd $GITHUB_WORKSPACE
          cmake --build build-tsan --parallel $(nproc)
      
      - name: Run tests with TSan
        run: |
          cd $GITHUB_WORKSPACE/build-tsan
          # Set TSan suppressions and run
          TSAN_OPTIONS="halt_on_error=1" ctest --verbose --output-on-failure || true

  # ════════════════════════════════════════════════════════════════════
  # RELEASE BUILD - Optimized
  # ════════════════════════════════════════════════════════════════════
  release-build:
    name: Release Build
    runs-on: ubuntu-latest
    needs: [ static-analysis, build-test ]
    if: success()
    
    steps:
      - uses: actions/checkout@v7
      
      - name: Setup build environment
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential cmake ninja-build \
            python3 python3-pip git curl \
            libcurl4-openssl-dev libssl-dev \
            libavcodec-dev libavformat-dev libavutil-dev \
            libswscale-dev libswresample-dev \
            libsdl2-dev libgl1-mesa-dev
          
          pip3 install --no-cache-dir conan
          conan profile detect --force
      
      - name: Conan install
        run: |
          cd $GITHUB_WORKSPACE
          conan install . --build=missing -s build_type=Release
      
      - name: CMake configure (Release)
        run: |
          cd $GITHUB_WORKSPACE
          cmake -B build-release -DCMAKE_TOOLCHAIN_FILE=build/Release/generators/conan_toolchain.cmake \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_FLAGS="-O3 -march=native"
      
      - name: Build Release
        run: |
          cd $GITHUB_WORKSPACE
          cmake --build build-release --parallel $(nproc)
      
      - name: Upload Release Binary
        uses: actions/upload-artifact@v4
        with:
          name: iptv-player-release-linux
          path: build-release/bin/iptv_player

  # ════════════════════════════════════════════════════════════════════
  # DOCKER BUILD - Production image
  # ════════════════════════════════════════════════════════════════════
  docker-build:
    name: Docker Build & Push
    runs-on: ubuntu-latest
    needs: [ build-test, release-build ]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v7
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      
      - name: Build Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile.prod
          push: false
          tags: iptv-player:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: Test Docker image
        run: |
          docker build -f Dockerfile.prod -t iptv-player:test .
          # Basic sanity check (just verify it starts)
          docker run --rm iptv-player:test || true

  # ════════════════════════════════════════════════════════════════════
  # SUMMARY
  # ════════════════════════════════════════════════════════════════════
  summary:
    name: Pipeline Summary
    runs-on: ubuntu-latest
    needs: [ static-analysis, code-coverage, build-test, thread-safety, release-build, docker-build ]
    if: always()
    
    steps:
      - name: Check pipeline status
        run: |
          echo "📊 CI/CD Pipeline Summary"
          echo "=========================="
          echo "✅ Static Analysis: ${{ needs.static-analysis.result }}"
          echo "✅ Code Coverage: ${{ needs.code-coverage.result }}"
          echo "✅ Build & Test: ${{ needs.build-test.result }}"
          echo "✅ Thread Safety: ${{ needs.thread-safety.result }}"
          echo "✅ Release Build: ${{ needs.release-build.result }}"
          echo "✅ Docker Build: ${{ needs.docker-build.result }}"
          
          if [ "${{ needs.static-analysis.result }}" != "success" ] || \
             [ "${{ needs.build-test.result }}" != "success" ] || \
             [ "${{ needs.thread-safety.result }}" != "success" ]; then
            echo "❌ Pipeline FAILED"
            exit 1
          fi
          
          echo "✅ Pipeline PASSED"